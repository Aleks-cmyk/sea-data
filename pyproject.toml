[project]
name = "sea-data"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Christian Rankl", email = "christian.rankl@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
sea-data = "sea_data:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy>=2.3.1",
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
